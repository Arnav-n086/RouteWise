# Optional: only needed if you want router.py's grey-zone HuggingFace
# classifier to actually run. Without these installed, grey-zone queries
# fall back to the rule-based score instead (see src/router.py) — this is
# the default for Docker and is not a degraded/broken mode, just a fallback.
#
# Install with: pip install -r requirements.txt -r requirements-ml.txt

transformers>=4.40.0
torch>=2.2.0
